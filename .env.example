# Supabase Configuration
# =====================================================================
# Copy this file to .env and fill in your actual values
# Get these from: https://app.supabase.com/project/_/settings/api
# =====================================================================

# Required: Supabase Project URL
# Example: https://abcdefghijklmnop.supabase.co
SUPABASE_URL=your_supabase_project_url_here

# Required: Supabase Anonymous Key (public key, safe to use in frontend)
# This key enforces Row-Level Security policies
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Optional: Service Role Key (DANGER: Full admin access, never commit!)
# Only use this for server-side admin operations
# NEVER expose this in frontend code or commit to git
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: Application Configuration
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO

# =====================================================================
# IMPORTANT SECURITY NOTES
# =====================================================================
# 1. Never commit .env file to git (it's in .gitignore)
# 2. Use SUPABASE_ANON_KEY for normal operations (enforces RLS)
# 3. Only use SERVICE_ROLE_KEY for admin scripts (bypasses RLS)
# 4. Rotate keys if accidentally exposed
# 5. Use different projects for development and production
# =====================================================================
